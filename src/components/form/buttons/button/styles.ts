import { Button } from '@mui/material';
import { styled } from '@mui/system';

export const CustomButton = styled(Button)({
	padding: '1rem 2rem',
	fontFamily: 'inherit',
	fontWeight: 'lighter',
	width: '80%',
	margin: '1.5rem 0'
});